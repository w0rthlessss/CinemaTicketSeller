<Window x:Class="CinemaTicketSeller.UserControls.RandomScreeningGenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaTicketSeller.UserControls"
        mc:Ignorable="d"
        Title="Генерация сеансов" Height="500" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2.5*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Label Content="Выберите дату" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="28"/>
        <Calendar x:Name="datePicker" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Border x:Name="apply" Background="#FF2AFF5D" Height="50" Width="150" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,50,50">
            <Label Content="Приянть" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
